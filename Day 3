// FIBONACII SERIES
class Solution {
public:
    int fib(int n) {
        // if(n==0 ) return 0;
        // if(n==1) return 1;
        if(n<=1) return n;
        
        //or simply by one line
        //return n>=1 ? n : fib(n-1) + fib(n-2);
        
        return fib(n-1)+fib(n-2);
    }
};
